<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heef.halo.domain.basic.mapper.AuthRolePermissionMapper">

    <!-- 通用查询字段列表 -->
    <sql id="Base_Column_List">
        id
        ,        role_id,        permission_id,        created_by,        created_time,        update_by,        update_time,        is_deleted    </sql>

    <!-- 分页查询角色权限关联表数据 -->
    <select id="selectPage" resultType="com.heef.halo.domain.basic.entity.AuthRolePermission">
        SELECT
        <include refid="Base_Column_List"/>
        FROM auth_role_permission
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.roleId != null and param.roleId != ''">
                    AND role_id = #{param.roleId}
                </if>
                <if test="param.permissionId != null and param.permissionId != ''">
                    AND permission_id = #{param.permissionId}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 条件查询角色权限关联表列表 -->
    <select id="selectList" resultType="com.heef.halo.domain.basic.entity.AuthRolePermission">
        SELECT
        <include refid="Base_Column_List"/>
        FROM auth_role_permission
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.roleId != null and param.roleId != ''">
                    AND role_id = #{param.roleId}
                </if>
                <if test="param.permissionId != null and param.permissionId != ''">
                    AND permission_id = #{param.permissionId}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <!-- 根据ID查询角色权限关联表详情 -->
    <select id="selectById" resultType="com.heef.halo.domain.basic.entity.AuthRolePermission">
        SELECT
        <include refid="Base_Column_List"/>
        FROM auth_role_permission
        WHERE id = #{id}
    </select>

    <!-- 新增角色权限关联表数据 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO auth_role_permission (role_id, permission_id, created_by, created_time, update_by, update_time,
                                          is_deleted)
        VALUES (#{entity.roleId}, #{entity.permissionId}, #{entity.createdBy}, #{entity.createdTime},
                #{entity.updateBy}, #{entity.updateTime}, #{entity.isDeleted})
    </insert>

    <!-- 批量新增角色权限关联表数据 -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO auth_role_permission (
        role_id, permission_id, created_by, created_time, update_by, update_time, is_deleted ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.roleId}, #{item.permissionId}, #{item.createdBy}, #{item.createdTime}, #{item.updateBy},
            #{item.updateTime}, #{item.isDeleted} )
        </foreach>
    </insert>

    <!-- 更新角色权限关联表数据 -->
    <update id="update">
        UPDATE auth_role_permission
        <set>
            <if test="entity.roleId != null">
                role_id = #{entity.roleId},
            </if>
            <if test="entity.permissionId != null">
                permission_id = #{entity.permissionId},
            </if>
            <if test="entity.createdBy != null">
                created_by = #{entity.createdBy},
            </if>
            <if test="entity.createdTime != null">
                created_time = #{entity.createdTime},
            </if>
            <if test="entity.updateBy != null">
                update_by = #{entity.updateBy},
            </if>
            <if test="entity.updateTime != null">
                update_time = #{entity.updateTime},
            </if>
            <if test="entity.isDeleted != null">
                is_deleted = #{entity.isDeleted}
            </if>
        </set>
        WHERE id = #{entity.id}
    </update>

    <!-- 根据ID删除角色权限关联表数据 -->
    <delete id="deleteById">
        DELETE
        FROM auth_role_permission
        WHERE id = #{id}
    </delete>

    <!-- 批量删除角色权限关联表数据 -->
    <delete id="deleteBatch">
        DELETE FROM auth_role_permission
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 统计角色权限关联表数据数量 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM auth_role_permission
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.roleId != null and param.roleId != ''">
                    AND role_id = #{param.roleId}
                </if>
                <if test="param.permissionId != null and param.permissionId != ''">
                    AND permission_id = #{param.permissionId}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
    </select>

    <!-- 根据ID判断角色权限关联表是否存在 -->
    <select id="existsById" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM auth_role_permission
        WHERE id = #{id}
    </select>

</mapper>
