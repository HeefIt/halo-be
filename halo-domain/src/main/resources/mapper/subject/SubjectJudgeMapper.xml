<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heef.halo.domain.basic.mapper.SubjectJudgeMapper">

    <!-- 通用查询字段列表 -->
    <sql id="Base_Column_List">
        id
        ,        subject_id,        is_correct,        created_by,        created_time,        update_by,        update_time,        is_deleted    </sql>

    <!-- 分页查询判断题数据 -->
    <select id="selectPage" resultType="com.heef.halo.domain.basic.entity.SubjectJudge">
        SELECT
        <include refid="Base_Column_List"/>
        FROM subject_judge
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.subjectId != null and param.subjectId != ''">
                    AND subject_id = #{param.subjectId}
                </if>
                <if test="param.isCorrect != null and param.isCorrect != ''">
                    AND is_correct = #{param.isCorrect}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <!-- 条件查询判断题列表 -->
    <select id="selectList" resultType="com.heef.halo.domain.basic.entity.SubjectJudge">
        SELECT
        <include refid="Base_Column_List"/>
        FROM subject_judge
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.subjectId != null and param.subjectId != ''">
                    AND subject_id = #{param.subjectId}
                </if>
                <if test="param.isCorrect != null and param.isCorrect != ''">
                    AND is_correct = #{param.isCorrect}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <!-- 根据ID查询判断题详情 -->
    <select id="selectById" resultType="com.heef.halo.domain.basic.entity.SubjectJudge">
        SELECT
        <include refid="Base_Column_List"/>
        FROM subject_judge
        WHERE id = #{id}
    </select>

    <!-- 新增判断题数据 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO subject_judge (subject_id, is_correct, created_by, created_time, update_by, update_time, is_deleted)
        VALUES (#{entity.subjectId}, #{entity.isCorrect}, #{entity.createdBy}, #{entity.createdTime},
                #{entity.updateBy}, #{entity.updateTime}, #{entity.isDeleted})
    </insert>

    <!-- 批量新增判断题数据 -->
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO subject_judge (
        subject_id, is_correct, created_by, created_time, update_by, update_time, is_deleted ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.subjectId}, #{item.isCorrect}, #{item.createdBy}, #{item.createdTime}, #{item.updateBy},
            #{item.updateTime}, #{item.isDeleted} )
        </foreach>
    </insert>

    <!-- 更新判断题数据 -->
    <update id="update">
        UPDATE subject_judge
        <set>
            <if test="entity.subjectId != null">
                subject_id = #{entity.subjectId},
            </if>
            <if test="entity.isCorrect != null">
                is_correct = #{entity.isCorrect},
            </if>
            <if test="entity.createdBy != null">
                created_by = #{entity.createdBy},
            </if>
            <if test="entity.createdTime != null">
                created_time = #{entity.createdTime},
            </if>
            <if test="entity.updateBy != null">
                update_by = #{entity.updateBy},
            </if>
            <if test="entity.updateTime != null">
                update_time = #{entity.updateTime},
            </if>
            <if test="entity.isDeleted != null">
                is_deleted = #{entity.isDeleted}
            </if>
        </set>
        WHERE id = #{entity.id}
    </update>

    <!-- 根据ID删除判断题数据 -->
    <delete id="deleteById">
        DELETE
        FROM subject_judge
        WHERE id = #{id}
    </delete>

    <!-- 批量删除判断题数据 -->
    <delete id="deleteBatch">
        DELETE FROM subject_judge
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 统计判断题数据数量 -->
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM subject_judge
        <where>
            <if test="param != null">
                <if test="param.id != null and param.id != ''">
                    AND id = #{param.id}
                </if>
                <if test="param.subjectId != null and param.subjectId != ''">
                    AND subject_id = #{param.subjectId}
                </if>
                <if test="param.isCorrect != null and param.isCorrect != ''">
                    AND is_correct = #{param.isCorrect}
                </if>
                <if test="param.createdBy != null and param.createdBy != ''">
                    AND created_by = #{param.createdBy}
                </if>
                <if test="param.createdTime != null and param.createdTime != ''">
                    AND created_time = #{param.createdTime}
                </if>
                <if test="param.updateBy != null and param.updateBy != ''">
                    AND update_by = #{param.updateBy}
                </if>
                <if test="param.updateTime != null and param.updateTime != ''">
                    AND update_time = #{param.updateTime}
                </if>
                <if test="param.isDeleted != null and param.isDeleted != ''">
                    AND is_deleted = #{param.isDeleted}
                </if>
            </if>
        </where>
    </select>

    <!-- 根据ID判断判断题是否存在 -->
    <select id="existsById" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM subject_judge
        WHERE id = #{id}
    </select>

    <!--查询判断题详情信息-->
    <select id="queryJudge" resultType="com.heef.halo.domain.basic.entity.SubjectJudge">
        SELECT
        <include refid="Base_Column_List"/>
        FROM subject_judge
        WHERE subject_id = #{subjectId}
        AND is_deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
